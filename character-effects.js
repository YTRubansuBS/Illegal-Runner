/* Character renderer is intentionally disabled in this loader build.
   The customization UI remains intact. The game engine must stay isolated
   from fetch interception so Supabase login/authentication is not affected. */
window.IR_CHARACTER_DRAW = null
window.IR_CHARACTER_SET = null
