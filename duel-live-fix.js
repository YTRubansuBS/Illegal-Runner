/* ILLEGAL RUNNER — duel visual compatibility layer
   The duel session transition and ghost are handled by duel-system.js.
   This file intentionally does not poll accepted sessions, which prevents
   old duels from reopening when a player logs in. */
(()=>{'use strict'})()
